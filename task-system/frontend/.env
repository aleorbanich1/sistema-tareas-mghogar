# App directo a Supabase (sin servidor). Se inyecta en build-time.
VITE_SUPABASE_URL=https://qsewancpibyyakitwpnr.supabase.co

# Pegá acá tu anon/public key (Project Settings → API → anon public).
# Es segura para el cliente. NO uses NUNCA la service_role aquí.
VITE_SUPABASE_ANON_KEY=PEGAR_ANON_KEY_AQUI
